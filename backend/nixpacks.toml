# Si Railway utilise Nixpacks (sans Dockerfile) : installe Chromium au build après npm ci.

[phases.install]
cmds = [
  "npm ci",
  "npx playwright install --with-deps chromium",
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.js"
