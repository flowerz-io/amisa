import SwiftUI

extension Color {
    /// Fond principal Home light (#EFEEF2) pour éviter le blanc pur à l’overscroll.
    static let homeLightChrome = Color(red: 239 / 255, green: 238 / 255, blue: 242 / 255)
}
