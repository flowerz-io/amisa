import SwiftUI

extension Color {
    /// Fond Home mode clair — papier chaud discret (#F4F1EE), évite blanc pur tout en restant neutre.
    static let homeLightChrome = Color(red: 244 / 255, green: 241 / 255, blue: 238 / 255)
}
